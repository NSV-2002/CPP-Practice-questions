// Write a C++ program to check whether a number can be expressed as sum of two primenumbers
#include<iostream>
using namespace std;
int main()
{
    int num;
    cout << "Enter  a number to check whether it can be expressed as sum of two primenumbers :-"<endl;
    cin >> num;
    for(int i=2;i<num;i++)
    {
        if(num%i==0)
        {
            
        }
    }
}